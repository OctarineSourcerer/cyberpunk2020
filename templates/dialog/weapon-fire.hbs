{{!-- Dialog content for when a ranged weapon is fired --}}
{{!-- By this point, fire mode and range should be selected, and possibly target --}}
<form class="cyberpunk weapon-modifiers">
    {{!-- Custom selector here because we're using translation parameters --}}
    {{!-- Select attack range --}}
    {{>(template "fields/select") label="Range" edit="range" choices=ranges localizationData=rangesLocal}}
</div>

    {{!-- Modifiers depending on players' actions --}}
    <ol class="field-list" style="grid-area: player;">
        {{>(template "fields/select") label="Aiming" edit="aimRounds" choices=ranges noTranslateOptions=true}}
    </ol>
</form>