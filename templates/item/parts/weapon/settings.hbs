<div class="field-list">
    {{> (template "fields/number") loc="Accuracy" path="data.accuracy"}}
    {{> (template "fields/text") loc="Damage" path="data.damage"}}
    {{> (template "fields/number") loc="Shots" path="data.shots"}}
    {{> (template "fields/boolean") loc="AP" path="data.ap"}}
    {{> (template "fields/number") loc="RoF" path="data.rof"}}
    {{> (template "fields/select") loc="Reliability" path="data.reliability" choices=reliabilities}}
    {{> (template "fields/number") loc="Range" path="data.range"}}
    {{> (template "fields/text") loc="AmmoType" path="data.ammoType"}}

    {{> (template "fields/select") loc="WeaponType" path="data.weaponType" choices=weaponTypes}}
    {{> (template "fields/select") loc="AttackType" path="data.attackType" choices=attackTypes}}
    {{> (template "fields/select") loc="Concealability" path="data.concealability" choices=concealabilities}}

    {{> (template "fields/select") loc="Availability" path="data.availability" choices=availabilities}}
</div>