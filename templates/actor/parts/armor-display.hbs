<div class="armor-display">
    {{#each hitLocations as |hitLoc name|}}
    <div class="armor-segment field" style="grid-area: {{name}}">
        {{#if hitLoc.location}}
        <label class="segment-loc" title="{{CPLocal "HitLoc"}}">
            <p class="inactive">{{displayRange hitLoc.location}}</p>
        </label>
        {{/if}}
        
        {{!-- Wow doing an input really breaks things --}}
        {{#unless ../edit}}
        <div class="segment-sp" title="{{CPLocal "SPFull"}}">{{hitLoc.stoppingPower}}</div>
        {{else}}
        <input class="segment-sp" title="{{CPLocal "SPFull"}}" name="data.coverage.{{name}}.stoppingPower" value="{{hitLoc.stoppingPower}}">
        {{/unless}}
        <label class="segment-name">{{shortCPLocal name}}</label>
    </div>
    {{/each}}
</div>