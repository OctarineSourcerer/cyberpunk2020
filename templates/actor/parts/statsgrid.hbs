{{!-- Container for all the stats --}}
<div class="flexcol">
    <div class="statsgrid">
        {{#each data.stats as |stat key|}}
        <div class="stat">
            <div class="field-label">{{localize (localizeStat key)}}</div>
            <div class="flexrow">
                <input type="number" name="data.stats.{{key}}.base" value="{{stat.base}}" class="stat-base"/>
                <div class="stat-mod">+</div>
                <input type="number" name="data.stats.{{key}}.tempMod" value={{stat.tempMod}} class="stat-temp"/>
            </div>
            <div class="stat-total action stat-roll" data-stat-name="{{key}}">{{stat.total}}</div>
        </div>
        {{/each}}
    </div>
    
    {{!-- Run, Leap etc. These are below, cos we're a lot less starved for vertical space than horizontal --}}
    {{!-- If I had horizontal space, I'd just slap these against their relevant stats --}}
    <div class="derivedrow">
        <div class="statderive">
            <div class="label">{{localize 'CYBERPUNK.BTmod'}}</div>
            <div class="value not-editable">{{data.stats.bt.modifier}}</div>
        </div>
        <div class="statderive">
            <div class="label">{{localize 'CYBERPUNK.Carry'}}</div>
            <div class="value not-editable">{{data.stats.bt.carry}}</div>
        </div>
        <div class="statderive liftstat">
            <div class="label">{{localize 'CYBERPUNK.Lift'}}</div>
            <div class="value rightborder not-editable">{{data.stats.bt.lift}}</div>
        </div>
        <div class="statderive">
            <div class="label">{{localize 'CYBERPUNK.Leap'}}</div>
            <div class="value not-editable">{{data.stats.ma.leap}}</div>
        </div>
        <div class="statderive">
            <div class="label">{{localize 'CYBERPUNK.Run'}}</div>
            <div class="value rightborder not-editable">{{data.stats.ma.run}}</div>
        </div>
    </div>
</div>